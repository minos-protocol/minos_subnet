# Pinned to the current stable validator stack (bittensor 10.3.1 pulls
# bittensor-drand>=1.3.0; pin drand too so the commit-reveal path is fixed).
# bittensor-cli 9.21.1 is the CLI that matches this SDK: 9.21.0 requires
# async-substrate-interface==2.0.2, which bittensor 10.3.1 excludes (>=2.0.3),
# so the two cannot be installed together.
# No torch: bittensor declares it only under its optional [torch] extra and
# lazy-loads it behind USE_TORCH=1, which nothing here sets — the validator
# submits weights as numpy arrays. Re-adding it costs every operator a
# multi-hundred-MB wheel for an import that never happens.
bittensor==10.3.1
bittensor-drand==1.3.0
bittensor-cli==9.21.1
numpy>=1.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
boto3>=1.28.0
httpx>=0.24.0
pysam>=0.21.0
tqdm>=4.65.0
rich>=13.0.0
questionary>=2.0.0